# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Chain orchestrator runtime state (MOD-010 CLI — operator-written ledger/portfolio)
runtime/

# Secrets — Alpaca PAPER API keys (KA-008 credential isolation; NEVER commit).
# Keys live in env or this git-ignored file ONLY — never in the repo, a memory file, or a node.
.secrets
.secrets/
*.secrets

