# Claude Code prompt — produce ADR-014 v2 (accept-ready, locks + audit folded, A8 resolved)

Paste the block below into a fresh Claude Code session rooted in `el_nino`. This is a **governance/design revision only — no `src/` code.** It revises the existing ADR-014 *draft* in place (same `canonical_id: ADR-014`, still `status: draft` for the operator to accept) into a complete, coherent v2 that folds the Q1–Q6 design locks, scopes the app_audit findings, and resolves the A8 contradiction — so it is a clean, accept-ready input to `/prd`.

---

Revise **ADR-014 "Operable Alpaca Paper Execution Adapter v1"** into **v2**. The current draft predates a six-round design grilling and a full code audit; v2 must fold both in. Keep it `status: draft` (the operator accepts it after review), `canonical_id: ADR-014` unchanged — this is an in-place revision of an unaccepted draft, not a supersession. **No `src/`/`tests/` changes; the refactor itself is downstream (`/prd-to-issues` → slices).**

## Step 0 — read and ground

1. `dev_graph/CLAUDE.md` — ADR node format, frontmatter, the writeback checklist, the 11 lint checks, Neo4j re-sync.
2. The current ADR-014 draft (locate it — check `dev_graph/decisions/` first, then repo root) and `app_audit.md` (the canonical audit — read its Part 2 per-check classifications and Part 3 blockers/warnings; the findings to fold are listed below in case the file isn't in the tree).
3. The code the locks reference, for accuracy: `src/execution/{engine,adapters,models,config,runtime}.py`, `src/orchestration/{engine,runtime}.py`, `src/gold/paper_runtime/{engine,predicates,models}.py`, the `gold_price`/`gold_price_proxy` feature, BENCH-004/006; plus ADR-011 (D1/D2, gate f), ADR-012, ADR-013.

## Step 1 — fold the Q1–Q6 design locks

Rewrite the Decision (and the current-state-vs-delta table, Consequences, Non-Goals) to encode these locks. The §1 spine is unchanged: **additive, simulator-unchanged, broker-realism quarantined**; what changes is *where* and *how*, per the locks.

- **Q1 — GLD price reference (the A8 fix), el_niño-side, source-pluggable.** Replace §5's "one contained shared correction" framing: `exec_ref_gld_price` is a *source-pluggable* execution reference — **simulator/replay path** uses a versioned deterministic **derived proxy** (`gold_price_proxy × oz_per_share`, a versioned constant; `gold_price_proxy` is real XAUUSD spot); **live Alpaca path** uses the **real live GLD mark** (read live, non-replayable). `gold_price` stays decision-context only. Additive field + corrected slippage + `execution_policy_version` (or `exec_price_source_version`) bump + BENCH-004/006 re-pin. **No Mr-Ripley dependency** (see Step 3).
- **Q2 — fork-(b) parallel live entrypoint.** Live operability is a **separate non-replayable entrypoint** (`live_runtime.py` / `operate_live`, mirroring `runtime.py`), NOT "behind the same `fill()` port." `ExecutionPort.fill()` stays the LONG-only replay contract, untouched, exercised only by the simulator. Correct §1/§6 wording from "behind the same port" → "behind the same module boundary, separate non-replayable entrypoint." The live path **reuses the deterministic decision + GATE-001 guard + idempotency layer; only broker mechanics are new.** Slippage: synthetic/derived ref → sim only (model number, labeled); real broker GLD mark → live only (recorded-not-modeled), with the reference **timestamp pinned + labeled** (submit/EOD mark = includes overnight gap; open/routing mark = pure execution slippage) and the no-liquidity-impact paper caveat noted.
- **Q3 — reconcile authority, heal, idempotency bifurcation, discrepancy.** Broker = authority for *position truth*; local SCHEMA-015 = authority for *decision lineage + idempotency*. Divergence heals via a **new append-only entry kind** (NOT an `ExecutionRecord`/`ExecutionEntry` — both require `source_record_id`, and the record requires `guard_result`, which a reconcile lacks) recording observed broker **qty + avg_entry_price** + a reconcile marker; live-path-only. Once-ever guard is **bifurcated, not demoted**: `has_execution(source_snapshot_id)` is the sole idempotency on the sim/replay path (crash-consistency depends on it); reconcile-delta + open-orders + `client_order_id` 422 own the live path. On discrepancy, **terminal-refuse ALL execution (no auto-flatten** — it imports a real-money instinct and destroys evidence); "refuse" scopes to execution only — the decision/observation/labeling chain keeps running (calibration runs on decisions, not fills), so the corpus doesn't stall. Governed adopt (ops-console-gated) only for adopting an unexplained non-zero broker position. **Load-bearing prerequisite:** the live path threads a **separate (ledger, portfolio) pair** — both the SCHEMA-013 ledger and the SCHEMA-015 portfolio — physically distinct from the canonical replay files (because `run_once` threads caller-chosen paths regardless of port). Enforce by routing non-replayable ports only through `live_runtime` **and** a cheap `assert port.replayable` precondition on the canonical `run_once`.
- **Q4 — in-flight window ownership.** Cross-snapshot in-flight/queue window is owned by **open-orders-aware reconcile**: read **positions ∪ open orders**; `delta = target − position − Σ(expected-lineage open-order qty)`; a queued order nets to zero → NO_ACTION. Two windows, two owners: open-orders-aware reconcile = cross-snapshot queue; `client_order_id` 422 = same-order duplicate-submit only. Unexpected open order (wrong qty/side/unknown `client_order_id`) is **not** netted → Q3 discrepancy → terminal-refuse → governed adopt. Motivation: the weekend case is foreclosed by `operational_ok`; the real trigger is **>1 ADMITting snapshot between two opens** (catch-up/backfill/manual multi-run) — rare, severe, closed structurally not by timing.
- **Q5 — sizing, cash-cap.** Cash-cap binds to the **literal settled `cash`** field from `GET /v2/account` — **never any `*_buying_power`** (makes "no margin" structural on any account type). Fixed-notional is **live-plug-only**; the sim keeps fixed-qty `default_size` (notional on the sim path changes `state_hash` → breaks BENCH). Effective reconcile target is cash-capped: `target = min(configured_notional, settled_cash)` (so Q4's reconcile converges). Accept the sim(fixed-qty) vs live(fixed-notional) asymmetry — diverges P&L magnitude, not direction. Fractionable (B2): `fractionable=true` → notional market+day; `false` → integer shares = `floor(cash-capped_notional / live_submit_mark)`. Live-plug-only.
- **Q6 — accumulate-only sim, sell-fold, side-based live adapter.** The simulated/replay portfolio is a **determinism vehicle, buy-only, never realizes — by design** (teaching the sim to sell breaks BENCH); realized P&L and exits are live-only. Its value/`unrealized_pnl` is a **Q2-class plausible-but-wrong artifact** — label it non-strategic, keep it out of every performance/ops-console reading. The **sell-fold lives in `live_runtime`, never `execute()`**: `_apply_sell` → `realized_pnl += qty × (exit_fill − avg_cost)`, `position → 0`. The **live adapter is side/order-based** (`submit_market_buy`/`submit_market_sell`) driven by the reconcile delta — LONG→buy, FLAT→sell, AVOID/WATCH→NO_ACTION resolved before the adapter; the v0 `AlpacaPaperAdapter.fill(direction)` (LONG-only) is superseded on the live path under fork-(b) — repurpose/remove it, don't extend it.

## Step 2 — scope the audit's ungoverned findings (decide v1 vs deferred, each with a one-line rationale)

Add these to the Decision/Defects so none falls through (the audit found them un-owned by any ADR):

- **Defect — sync-fill raise (high, active-bug face of the QUEUED gap):** the v0 adapter raises unless `status ∈ {filled,FILLED}`, but real market orders return `accepted`/`pending_new` and fill async. Add to the enumerated defects; fix = the §6 reconcile/state-machine, resolving fills via `GET /v2/positions` + `GET /v2/orders` (note: there is **no** `GET /v2/orders/reconcile` endpoint), never the synchronous POST echo.
- **Defect — mislabeled "daily" guard inputs (dormant):** `daily_pnl` sums all-time realized P&L; `trades_today = next_seq()` = lifetime fill count (blocks forever after 10 fills). Fix threads the snapshot `as_of` into `build_guard_request` + an `as_of` field on `ExecutionEntry` (SCHEMA-015 change + BENCH re-pin), mirroring the PRED-008 cooldown's snapshot-`as_of` discipline. Scope into v1.
- **Uncaught HTTPError + discarded reject reason:** catch `HTTPError`/`URLError`, parse the body, route to EXECUTION_UNCERTAIN/halt + persist `raw_payload`; add a 4xx test (the suite only stubs `status='filled'`). v1.
- **429 / Retry-After (genuinely ungoverned):** honor `Retry-After`, no blind resend; add 429 to the §6 state machine. v1.
- **`GET /v2/account` status gate:** require `status=="ACTIVE" && !trading_blocked && !account_blocked` in the v1 startup reconcile; record it so it's governed. v1.
- **Operator kill switch (ungoverned):** add an audited Tier-2 halt (the feed already honors `halt=True`) **or** explicitly govern one-shot+unregister as the accepted kill mechanism. Pick one.
- **Broker-traceability fields:** `alpaca_order_id`, `raw_payload` additive on SCHEMA-014. v1.
- **Lower/deferred (record the decision):** early-close/half-days (immaterial at day granularity — document); clock-feed TZ normalization (add a near-midnight-UTC fixture, touch both feeds together); audit `actor`/who field (single-operator — record rationale or add); GDPR/CCPA "N-A" one-line governance note.

## Step 3 — resolve the A8 contradiction in favour of the derived-proxy decision

`app_audit.md` blocker #1 frames the price fix as **blocked on a Mr-Ripley (Layer-2) ingest** ("must land first") — a hard cross-repo predecessor. **v2 must overrule this**, per the Q1/Q2 locks: the price fix is **el_niño-side** (derived proxy on the sim path, live GLD mark on the Alpaca path) and needs **no Mr-Ripley change**. Re-scope §5 / blocker #1 accordingly: the defect is **real and high**, the fix is **el_niño-only with no hard cross-repo predecessor**, and the **real-GLD-in-snapshot ingest is demoted to an optional, separately-tracked simulator-fidelity upgrade** (behind the same pluggable interface, zero rework when/if it lands). Correct every "A8 blocks / Mr-Ripley first" statement to this.

## Step 4 — keep it accept-ready and PRD-clean

- Refresh the **current-state-vs-v1-delta table** and the **B-flag verification track** to reflect what the audit already closed (e.g. bare-numeric `qty` accepted; orders return async `accepted`; `GET /v2/calendar` behavior) and what stays open (B1 `client_order_id` length, B2 `fractionable`, B3 the settled-cash field/`multiplier`, the live order-lifecycle/positions/open-orders facts).
- Separate the Decision clearly into **(i) contained shared changes (versioned)** — the §5 price reference, the daily-guard `as_of` threading, the `assert port.replayable` guard, the additive SCHEMA-014/015 fields — and **(ii) live-plug-only additions** — reconcile/sell/state-machine/cash-cap/`client_order_id`/heal/side-based adapter. This split is what makes it a clean `/prd` slice source.
- Keep Status, Context, Decision, Alternatives Considered, Consequences, Out-of-scope, Relationships; mark it **v2** in the Status note with a one-line changelog (what the grilling + audit changed). `status: draft`, `decision_status: active`, `updated: <today>`.

## Step 5 — writeback (governance-only)

- Ensure ADR-014 lives as a governed node at `dev_graph/decisions/ADR - Operable Alpaca Paper Execution Adapter v1.md` (move it there if it's only at repo root), frontmatter valid; `index.md` Decisions row + Statistics; `log.md` entry (`ADR-014 v2 — locks + audit folded, A8 resolved`); run the 11 lint checks on touched nodes; **Neo4j re-sync**. **Status stays `draft`** — do not auto-accept; acceptance is the operator's checkpoint.
- No `src/`/`tests/` change; canonical_id unchanged; no `wiki/**`/`raw/**` mutation (CON-001).
- When done, summarize the v2 changelog (locks folded, findings scoped, A8 resolved) and confirm it's `draft`/accept-ready, and stop for operator review.
